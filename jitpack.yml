jdk:
  - openjdk17
before_install:
  - ./gradlew :sdk:clean
build:
  - ./gradlew :sdk:assembleRelease :sdk:generatePomFileForReleasePublication -x :demo:build
install:
  - ./gradlew :sdk:publishToMavenLocal -x :demo:build

